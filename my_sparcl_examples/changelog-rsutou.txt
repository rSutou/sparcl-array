Value.hs:
	Data.Array.IO の importを追加
	data Value の定義に純粋な(スライスではない)配列型を追加
	IOArrayと配列の長さ(後でスライスとの変換で必要になるパラメータ)

	Eval a を readerT int IO a に変更
	それに伴い, 以下の部分を変更

	Value.hsの
		runEval の型と実装
		evalTest の実装

	Module.hsの
		readModuleの引数の型
		readModuleの引数interpを使用している個所
		interpModuleWorksの引数の型

	REPL.hsの
		procload内のreadModuleに渡す関数を内の適用($)をfmap(<$>)に

		evalExp内
			演算子を適切に??? (=<<) に変更
			evaluate関数をこのように使って本来の目的に沿っているのかは自身が無い

この段階で型エラーは消えたが, 
cabal run はできた
:load してやったらエラー	
Unexpected exception is thrown.
cyclic evaluation in fixIO

readModuleと
interpModuleWorks で相互再帰している部分に IO を使っているのが問題か?

	readModuleの引数interp の型の部分を IO ではなく M v m にしてみた
	interpModuleWorksも同様に変更した
	REPL.hs内でreadModuleを呼び出してるところも変更が必要
	 readModuleに渡す関数内でliftIOをするように変更
変わらなかった. 
	よく考えたらliftIOのタイミングが違うだけだった
IOに戻した

このエラーの原因はreadModule とinterpModuleWorksの再帰だと思うが, 
理解のためにはこのIOモナドがつかわれている場所を見るのもよさそうである

仮説1	
	Module.hs内の
    -- let newValueEnv = interp valEnv bind
    -- newValueEnv <- liftIO $ interp valEnv bind
	と変更した部分において, 遅延されるべき計算がアクションとしてこのタイミングで実行することを強制されているのが問題か?

仮説2
	EVAL a が ReaderT IO になってしまったことによるサイクルか? 
	EvalUBindのrecのところが


nolet.sparcl に let 使っていないのを書いてloadしたら行けた.

Eval.hs
	EvalUBindのrecのところで, unsafeInterLeaveIOを使って強制的にアクションを遅延させることで解消,

recを含むプログラムをloodできた.


	

Todo
	スライス値を追加
		親配列の情報と, 